# docker compose 部署可选覆盖项（复制为 .env 放在 compose 同目录）
# 数据库密码（生产务必修改）
DB_PASSWORD=scenario
# JWT 密钥（生产务必修改为强随机值）
JWT_SECRET=change-me-in-prod
# 前端对外端口
WEB_PORT=3100
# 允许的前端来源（多个用逗号分隔）
CORS_ORIGINS=http://localhost:3100
